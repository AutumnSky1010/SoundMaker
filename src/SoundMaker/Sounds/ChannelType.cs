namespace SoundMaker.Sounds;
/// <summary>
/// the type which is expressed channels count of the sound. チャンネル数を表す列挙型
/// </summary>
public enum ChannelType
{
    /// <summary>
    /// monaural(1ch) モノラル1ch
    /// </summary>
    Monaural,
    /// <summary>
    /// stereo(2ch) ステレオ2ch
    /// </summary>
    Stereo
}